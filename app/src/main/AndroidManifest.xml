<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.CommicartAndroid"
        tools:targetApi="31">

        <activity
            android:name=".features.auth.presentation.RoleSelectionActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".features.auth.presentation.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.CommicartAndroid">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".features.auth.presentation.RegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".features.customer.presentation.CustomerDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".features.artist.presentation.ArtistDashboardActivity"
            android:exported="false" />
        <activity android:name=".features.profile.presentation.ProfileActivity" android:exported="false">

        </activity>
        <activity android:name=".core.base.DashboardActivity" android:exported="false">

        </activity>

        <!-- Artist Activities -->
        <activity
            android:name=".features.artist.presentation.PortfolioActivity"
            android:exported="false" />
        <activity
            android:name=".features.artist.presentation.CommissionsActivity"
            android:exported="false" />

        <!-- Customer Activities -->
        <activity
            android:name=".features.customer.presentation.MarketplaceActivity"
            android:exported="false" />
        <activity
            android:name=".features.customer.presentation.CommissionRequestActivity"
            android:exported="false" />


    </application>

</manifest>